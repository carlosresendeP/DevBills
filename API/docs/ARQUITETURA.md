# 🏗️ Arquitetura da ContaZero API

## 📐 Diagrama de Arquitetura

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENTE / FRONTEND                       │
│                    (React, Mobile App, etc.)                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP Requests
                             │ Authorization: Bearer <token>
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        FASTIFY SERVER                            │
│                      (http://localhost:3001)                     │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                    ROUTES LAYER                         │    │
│  │  ┌──────────────┐  ┌──────────────────────────────┐    │    │
│  │  │ /api/health  │  │  /api/categories             │    │    │
│  │  └──────────────┘  │  /api/transactions           │    │    │
│  │                    │  /api/transactions/summary   │    │    │
│  │                    │  /api/transactions/historical│    │    │
│  │                    └──────────────────────────────┘    │    │
│  └─────────────────────────┬──────────────────────────────┘    │
│                            │                                     │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │              MIDDLEWARE LAYER                          │    │
│  │  ┌──────────────────────────────────────────────┐     │    │
│  │  │         authMiddleware                        │     │    │
│  │  │  - Valida token Firebase                     │     │    │
│  │  │  - Extrai userId                             │     │    │
│  │  │  - Adiciona userId ao request                │     │    │
│  │  └──────────────────────────────────────────────┘     │    │
│  └─────────────────────────┬──────────────────────────────┘    │
│                            │                                     │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │              VALIDATION LAYER (Zod)                    │    │
│  │  - createTransactionSchema                             │    │
│  │  - getTransactionSchema                                │    │
│  │  - getTransactionSummarySchema                         │    │
│  │  - getHistoricalTransactionSchema                      │    │
│  │  - deleteTransactionSchema                             │    │
│  └─────────────────────────┬──────────────────────────────┘    │
│                            │                                     │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │              CONTROLLERS LAYER                         │    │
│  │  ┌────────────────────────────────────────────────┐   │    │
│  │  │  Category Controller                           │   │    │
│  │  │  - getCategories()                             │   │    │
│  │  └────────────────────────────────────────────────┘   │    │
│  │  ┌────────────────────────────────────────────────┐   │    │
│  │  │  Transaction Controllers                       │   │    │
│  │  │  - createTransaction()                         │   │    │
│  │  │  - getTransactions()                           │   │    │
│  │  │  - getTransactionsSummary()                    │   │    │
│  │  │  - getHistoricalTransaction()                  │   │    │
│  │  │  - deleteTransaction()                         │   │    │
│  │  └────────────────────────────────────────────────┘   │    │
│  └─────────────────────────┬──────────────────────────────┘    │
│                            │                                     │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │              SERVICES LAYER                            │    │
│  │  - inializeGlobalCategories()                          │    │
│  │  - Lógica de negócio compartilhada                     │    │
│  └─────────────────────────┬──────────────────────────────┘    │
│                            │                                     │
└────────────────────────────┼─────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐   ┌────────────────┐   ┌──────────────┐
│   FIREBASE    │   │     PRISMA     │   │    DAY.JS    │
│ Authentication│   │   ORM Layer    │   │Date Handling │
│               │   │                │   │              │
│ - verifyToken │   │ - transaction  │   │ - UTC dates  │
│ - extractUID  │   │ - category     │   │ - Formatting │
└───────────────┘   └────────┬───────┘   └──────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │    MONGODB      │
                    │  (Database)     │
                    │                 │
                    │ Collections:    │
                    │ - categories    │
                    │ - transactions  │
                    └─────────────────┘
```

---

## 🔄 Fluxo de Requisição Completo

### Exemplo: POST /api/transactions

```
1. CLIENTE
   └─► Envia POST com body e token
        │
        ▼
2. FASTIFY
   └─► Recebe requisição
        │
        ▼
3. MIDDLEWARE (authMiddleware)
   ├─► Extrai token do header
   ├─► Valida com Firebase Admin
   ├─► Extrai userId do token decodificado
   └─► Adiciona userId ao request.userId
        │
        ▼
4. VALIDATION (Zod)
   ├─► Valida body com createTransactionSchema
   ├─► description: string não vazio
   ├─► amount: number positivo
   ├─► date: data válida
   ├─► categoryId: ObjectId válido
   └─► type: INCOME ou EXPENSE
        │
        ▼
5. CONTROLLER (createTransaction)
   ├─► Verifica se usuário está autenticado
   ├─► Busca categoria no banco
   ├─► Valida se categoria existe e tipo corresponde
   ├─► Converte data para Date
   ├─► Cria transação via Prisma
   └─► Retorna transação criada
        │
        ▼
6. PRISMA ORM
   ├─► Converte operação para query MongoDB
   ├─► Executa insert
   └─► Retorna documento criado
        │
        ▼
7. MONGODB
   ├─► Armazena documento na collection transactions
   ├─► Gera ObjectId automaticamente
   └─► Retorna documento salvo
        │
        ▼
8. RESPOSTA AO CLIENTE
   └─► Status 201 + JSON da transação criada
```

---

## 🗂️ Estrutura de Camadas (Layered Architecture)

```
┌─────────────────────────────────────────────┐
│         PRESENTATION LAYER                  │  ← Rotas HTTP
│  - Routes (index.ts)                        │
│  - category.routes.ts                       │
│  - transation.routes.ts                     │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│         MIDDLEWARE LAYER                    │  ← Validação e Auth
│  - authMiddleware (Firebase)                │
│  - Schemas Zod (validação)                  │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│         BUSINESS LOGIC LAYER                │  ← Lógica de Negócio
│  - Controllers                              │
│  - Services                                 │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│         DATA ACCESS LAYER                   │  ← Acesso a Dados
│  - Prisma ORM                               │
│  - Types (TypeScript)                       │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│         DATABASE LAYER                      │  ← Banco de Dados
│  - MongoDB                                  │
│  - Collections (categories, transactions)   │
└─────────────────────────────────────────────┘
```

---

## 🔐 Fluxo de Autenticação

```
┌──────────────┐
│   CLIENTE    │
└──────┬───────┘
       │
       │ 1. Login com Firebase (frontend)
       ▼
┌─────────────────┐
│ FIREBASE AUTH   │
│  (Frontend SDK) │
└──────┬──────────┘
       │
       │ 2. Retorna ID Token
       ▼
┌──────────────┐
│   CLIENTE    │
└──────┬───────┘
       │
       │ 3. Envia requisição com token
       │    Authorization: Bearer <token>
       ▼
┌─────────────────────┐
│  authMiddleware     │
│  (Backend)          │
└──────┬──────────────┘
       │
       │ 4. Valida token
       ▼
┌─────────────────────┐
│ FIREBASE ADMIN SDK  │
│ verifyIdToken()     │
└──────┬──────────────┘
       │
       │ 5. Retorna decodedToken com uid
       ▼
┌─────────────────────┐
│  authMiddleware     │
│  request.userId =   │
│  decodedToken.uid   │
└──────┬──────────────┘
       │
       │ 6. Passa para o controller
       ▼
┌─────────────────────┐
│   CONTROLLER        │
│   Usa request.userId│
└─────────────────────┘
```

---

## 📊 Modelo de Dados (ER Diagram)

```
┌─────────────────────────────────────────┐
│             CATEGORY                    │
├─────────────────────────────────────────┤
│ id          : ObjectId (PK)             │
│ name        : String                    │
│ color       : String (#FF5733)          │
│ type        : enum (INCOME|EXPENSE)     │
│ createdAt   : DateTime                  │
│ updatedAt   : DateTime                  │
└──────────────────┬──────────────────────┘
                   │
                   │ 1:N
                   │
┌──────────────────▼──────────────────────┐
│           TRANSACTION                   │
├─────────────────────────────────────────┤
│ id          : ObjectId (PK)             │
│ description : String                    │
│ amount      : Float                     │
│ date        : DateTime                  │
│ type        : enum (INCOME|EXPENSE)     │
│ userId      : String (Firebase UID)     │
│ categoryId  : ObjectId (FK)             │────┐
│ createdAt   : DateTime                  │    │
│ updatedAt   : DateTime                  │    │
└─────────────────────────────────────────┘    │
                                               │
                   ┌───────────────────────────┘
                   │ Relacionamento
                   │ N:1
```

### Relacionamentos:

- **Category → Transaction**: 1:N (Uma categoria pode ter várias transações)
- **Transaction → Category**: N:1 (Cada transação pertence a uma categoria)

### Índices:

```
Transaction:
  - [userId, date]           → Buscar transações de um usuário por período
  - [userId, date, categoryId] → Filtros complexos

Category:
  - UNIQUE [name, type]      → Evitar categorias duplicadas
```

---

## 🧩 Componentes e Dependências

```
┌────────────────────────────────────────────────────────┐
│                    APPLICATION                         │
│                                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │
│  │   Fastify    │  │  TypeScript  │  │   Biome     │ │
│  │  (Server)    │  │   (Types)    │  │  (Linter)   │ │
│  └──────────────┘  └──────────────┘  └─────────────┘ │
│                                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │
│  │     Zod      │  │    Day.js    │  │    CORS     │ │
│  │ (Validation) │  │   (Dates)    │  │  (Headers)  │ │
│  └──────────────┘  └──────────────┘  └─────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │              AUTHENTICATION                       │ │
│  │  ┌──────────────────────────────────────┐        │ │
│  │  │      Firebase Admin SDK               │        │ │
│  │  │  - Authentication                     │        │ │
│  │  │  - Token Verification                 │        │ │
│  │  └──────────────────────────────────────┘        │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │              DATABASE ACCESS                      │ │
│  │  ┌──────────────────────────────────────┐        │ │
│  │  │           Prisma ORM                  │        │ │
│  │  │  - Type-safe queries                  │        │ │
│  │  │  - Schema management                  │        │ │
│  │  │  - Migrations                         │        │ │
│  │  └────────────────┬──────────────────────┘        │ │
│  └───────────────────┼───────────────────────────────┘ │
│                      │                                  │
└──────────────────────┼──────────────────────────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │    MONGODB      │
              │   (Database)    │
              └─────────────────┘
```

---

## 🔄 Ciclo de Vida da Aplicação

```
1. INICIALIZAÇÃO
   │
   ├─► Carrega variáveis .env
   │   └─► Valida com Zod (env.ts)
   │
   ├─► Inicializa Firebase Admin
   │   └─► Verifica credenciais
   │
   ├─► Conecta ao MongoDB via Prisma
   │   └─► Testa conexão
   │
   ├─► Inicializa Categorias Globais
   │   └─► Cria categorias padrão se não existirem
   │
   ├─► Configura Fastify
   │   ├─► Habilita CORS
   │   ├─► Configura Logger
   │   └─► Registra Rotas
   │
   └─► Inicia Servidor
       └─► Escuta na porta 3001

2. RUNTIME (Servidor Ativo)
   │
   ├─► Aguarda requisições HTTP
   │
   ├─► Para cada requisição:
   │   ├─► Passa pelos middlewares
   │   ├─► Valida dados
   │   ├─► Executa controller
   │   ├─► Interage com banco
   │   └─► Retorna resposta
   │
   └─► Logs de eventos e erros

3. SHUTDOWN
   │
   ├─► Encerra conexões ativas
   ├─► Fecha conexão com MongoDB
   └─► Finaliza processo
```

---

## 🎯 Principais Padrões Utilizados

### 1. **MVC (Model-View-Controller)**

```
Model      → Prisma Schema (schema.prisma)
View       → JSON Responses (API REST)
Controller → Controllers (*.controller.ts)
```

### 2. **Repository Pattern**

```
Prisma ORM atua como Repository Layer
Abstrai acesso ao MongoDB
```

### 3. **Middleware Pattern**

```
Intercepta requisições antes dos controllers
Exemplo: authMiddleware
```

### 4. **DTO (Data Transfer Object)**

```
Zod Schemas definem estrutura de dados
Validação e tipagem automática
```

### 5. **Dependency Injection**

```
Prisma Client injetado via import
Firebase Admin injetado via import
```

---

## 📈 Escalabilidade

### Pontos de Otimização:

1. **Índices no Banco**
   - [userId, date]
   - [userId, date, categoryId]

2. **Agregação Eficiente**
   - Uso de Map para agrupar dados

3. **Queries Otimizadas**
   - Select apenas campos necessários
   - Include seletivo de relacionamentos

4. **Validação em Camadas**
   - Zod (aplicação)
   - Prisma (ORM)
   - MongoDB (banco)

---

## 🔒 Segurança

### Camadas de Segurança:

```
1. Autenticação (Firebase)
   └─► Token JWT verificado em cada requisição

2. Autorização (userId)
   └─► Cada operação valida propriedade dos dados

3. Validação de Entrada (Zod)
   └─► Previne dados inválidos/maliciosos

4. Sanitização (Prisma)
   └─► Previne SQL/NoSQL Injection

5. CORS (Fastify)
   └─► Controla origens permitidas

6. HTTPS (Produção)
   └─► Criptografia em trânsito
```

---

## 📦 Módulos e Responsabilidades

| Módulo         | Responsabilidade                      |
| -------------- | ------------------------------------- |
| `app.ts`       | Configurar Fastify e plugins          |
| `server.ts`    | Inicializar aplicação                 |
| `Routes/`      | Definir endpoints HTTP                |
| `controllers/` | Lógica de negócio                     |
| `middlewares/` | Validação e autenticação              |
| `schemas/`     | Validação com Zod                     |
| `services/`    | Lógica compartilhada                  |
| `config/`      | Configurações (env, prisma, firebase) |
| `types/`       | Tipos TypeScript                      |

---

**Documentação de Arquitetura**
**Versão:** 1.0.0
**Data:** 26/10/2025
